# Package: api-client

V1 constraints apply across the repository:
- no proprietary font hosting/preview
- core is offline, no implicit network calls
- manifest is source of truth
- event log is append-only

Purpose:
- api-client (see directory name)

{
  "name": "@setzkasten/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo \"TODO: build @setzkasten/cli\" && exit 0",
    "test": "echo \"TODO: test @setzkasten/cli\" && exit 0",
    "lint": "echo \"TODO: lint @setzkasten/cli\" && exit 0"
  }
}
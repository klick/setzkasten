{
  "name": "@setzkasten/events",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo \"TODO: build @setzkasten/events\" && exit 0",
    "test": "echo \"TODO: test @setzkasten/events\" && exit 0",
    "lint": "echo \"TODO: lint @setzkasten/events\" && exit 0"
  }
}